import { CompletedfilterPipe } from './completedfilter.pipe';

describe('CompletedfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CompletedfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
